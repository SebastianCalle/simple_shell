#include "shell.h"

/**
 * no_found - default function
 * @argc: Number of arguments
 * @l: input line
 * @args: Multidimensional array of arguments
 * @link: Environment linked list
 * Return: 0 if is succes or -1 if not
 */
int no_found(UN int argc, UN char *l, UN char **args, UN node_t **link)
{
	return (-1);
}
